name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - numpy
  - PyYAML
  - scipy
  - networkx
  - rdkit
  - sympy
    # Pip-only installs
  - pip:
    - codecov
